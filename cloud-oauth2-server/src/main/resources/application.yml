server:
  port: 8080

spring:
  application:
    name: cloud-oauth2-server
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/oauth_demo?useSSL=false
    username: root
    password: root